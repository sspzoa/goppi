# Local development helpers. goppi does not load this file automatically;
# export variables in your shell or use `goppi login`.

UPSTAGE_API_KEY=
GOPPI_API_KEY=

# Optional overrides (see docs/development.md)
# GOPPI_DATA_DIR=~/.local/share/goppi
# GOPPI_BASE_URL=https://api.upstage.ai/v1
# GOPPI_SANDBOX=workspace|strict|off
# GOPPI_TUI=0

# Live Solar smoke (requires UPSTAGE_API_KEY):
# make live-check
# GOPPI_LIVE=1 go test ./internal/provider -run Live
